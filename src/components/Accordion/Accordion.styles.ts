// External imports.
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  headerRow: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  headerContainer: {
    flex: 1,
  },
  measuringContainer: {
    position: 'absolute',
    opacity: 0,
  },
});

export default styles;
